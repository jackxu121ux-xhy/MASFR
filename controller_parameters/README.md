# Controller Parameters

This directory is reserved for crawling, flight, and multimodal switching parameters used by MASFR experiments. Include versioned configuration files, gain tables, timing sequences, and notes linking each parameter set to the corresponding experiment.

